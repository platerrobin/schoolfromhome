/* Simple script to generate a worksheet PDF from a resource using jsPDF. This can be run locally
   (node scripts/generate_worksheet.js <resourceId>) after installing dependencies and setting up the DB.
*/

console.log('This script is a placeholder to generate teacher-custom worksheets. For on-demand worksheets, use the /api/worksheets/generate?resourceId=ID endpoint.')
